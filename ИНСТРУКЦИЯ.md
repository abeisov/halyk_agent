# Что делать 9 августа

## Шаг 1. Скачать приватный датасет (~11:00)

Скачиваешь архив с сайта конкурса. Распаковываешь и **содержимое** кладёшь в папку:

```
/Users/anuarbeisov/Desktop/halyk-covenant-agent/data/private/
```

Папка уже создана. Внутри должно получиться так:

```
data/private/
├── documents/                  ← папка с PDF
├── master_ledger_2025.csv      ← таблица (имя может отличаться)
└── submission_template.json    ← шаблон ответа
```

Если после распаковки получилась лишняя папка внутри (например
`data/private/private_dataset/documents/`) — перетащи содержимое на уровень выше,
чтобы `documents/` лежала прямо в `data/private/`.

## Шаг 2. Сказать мне

Пишешь в чат: **«файлы на месте, запускай»**.

Дальше я всё делаю сам: проверяю комплектность, гоняю пайплайн, смотрю логи,
чиню что вылезет. Занимает ~30-40 минут.

## Шаг 3. Что сдавать

**Сдаются две вещи.**

### 1. Файл `submission.json` — на сайт конкурса

Лежать будет здесь:

```
/Users/anuarbeisov/Desktop/halyk-covenant-agent/submission.json
```

Как открыть папку: Finder → Cmd+Shift+G → вставить путь
`/Users/anuarbeisov/Desktop/halyk-covenant-agent` → Enter.
Файл `submission.json` будет прямо в корне, рядом с `README.md`.

Я скажу «файл готов», когда он собран и прошёл валидацию. Размер ~10 КБ
(лимит сайта 20 МБ — с запасом).

**Три попытки загрузки, засчитывается лучшая.** План:
- отправка №1 — сразу как файл готов (ранняя отправка выигрывает при равенстве очков)
- отправка №2 — после того как я разберу логи и поправлю слабые места
- отправка №3 — держим в резерве до 13:40

### 2. Ссылка на репозиторий — если попросят

```
https://github.com/abeisov/halyk_agent
```

Там код, README с описанием подхода, история работы по коммитам.
Данных организаторов и ключей в репозитории нет. Это идёт в критерий
«качество решения и кода».

## Если что-то пойдёт не так

Пиши мне что видишь — я разберусь. Пайплайн устроен так, что даже при
падении отдельных кусков он доходит до конца и выдаёт заполненный файл:
пустая ячейка — гарантированный ноль, а заполненная хоть чем-то — шанс на балл.

## Таймлайн

| Время | Что |
|---|---|
| 10:30 | я делаю контрольный прогон на публичных данных |
| 11:00 | ты скачиваешь приватный датасет, кладёшь в `data/private/` |
| 11:20 | прогон |
| ~12:00 | **отправка №1** |
| 12:00–13:30 | правки по логам |
| 13:30 | **отправка №2** |
| 13:45 | стоп, финальная проверка |
| 14:00 | дедлайн |
